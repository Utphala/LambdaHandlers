package edu.uta.cse.lambda;

import edu.uta.cse.lambda.requests.BasicInfo;

public class Utils {
    public static final boolean isSessionValid(final BasicInfo info) {
        boolean isValid = false;
        String cookie = info.getCookie();
        
        
        return isValid;
    }
}
